# 钉钉文档同步区

此目录用于存放从钉钉在线文档同步过来的内容。

## 工作方式

1. 配置钉钉 MCP（在 `.cursor/mcp.json` 中添加钉钉 MCP 服务）
2. AI 可通过 MCP 工具直接读取钉钉文档
3. 可将钉钉文档内容导出为本地 Markdown 文件保存在此目录

## 目录规范

建议按钉钉空间/知识库组织子目录：

```
dingtalk-docs/
├── {空间名}/
│   ├── {文档名}.md
│   └── ...
└── ...
```

## 待配置

等待钉钉 MCP 信息补充，配置完成后此目录将自动与钉钉在线文档联动。
